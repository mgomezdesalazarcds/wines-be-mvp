# Local Postgres for development. Once the schema settles, this moves to a
# fresh Supabase project (same DATABASE_URL/DIRECT_URL split as wines-be-node:
# pooler for the app, direct connection for migrations).
DATABASE_URL="postgresql://codigodelsur1@localhost:5432/wines_mvp_dev"
DIRECT_URL="postgresql://codigodelsur1@localhost:5432/wines_mvp_dev"

PORT=3002

GEMINI_API_KEY=
CORS_ORIGIN=http://localhost:5173
